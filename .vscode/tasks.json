{
    "tasks": [
        {
            "type": "shell",
            "label": "buildWithMakefile",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build"
        }
    ],
    "version": "2.0.0"
}
